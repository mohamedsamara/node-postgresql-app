import { expect } from 'chai';

describe('state example', () => {
  it('example one', () => {
    const state = { counter: 0 };
    expect(state.counter).to.equal(0);
  });
  it('example 2', () => {
    const state = { counter: 0 };
    expect(state.counter).to.equal(0);
  });
});
